import { useStyles } from "./style";

export const Description = () => {
    const classes = useStyles();
    return (
        <div className={classes.description}>Arrange the words in the correct order</div>

    );

}