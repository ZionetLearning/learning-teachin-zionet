apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.learningManager.name }}
  namespace: {{ include "app.namespace" . }}
  labels:
    {{ .Values.learningManager.labels.selectorKey }}: {{ .Values.learningManager.labels.selectorValue }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{ .Values.learningManager.labels.selectorKey }}: {{ .Values.learningManager.labels.selectorValue }}
  template:
    metadata:
      labels:
        {{ .Values.learningManager.labels.selectorKey }}: {{ .Values.learningManager.labels.selectorValue }}
      annotations:
        {{ include "app.dapr.annotations" (dict
            "enabled" .Values.global.dapr.enabled
            "appId" (.Values.learningManager.dapr.appId | default .Values.learningManager.name)
            "appPort" .Values.learningManager.dapr.appPort
            "config"  (.Values.global.dapr.configName | default "dapr-config")
        ) | nindent 8 }}
        dapr.io/sidecar-cpu-request:  "{{ .Values.global.dapr.sidecar.requests.cpu }}"
        dapr.io/sidecar-memory-request: "{{ .Values.global.dapr.sidecar.requests.memory }}"
        dapr.io/sidecar-cpu-limit:    "{{ .Values.global.dapr.sidecar.limits.cpu }}"
        dapr.io/sidecar-memory-limit: "{{ .Values.global.dapr.sidecar.limits.memory }}"
    spec:
      {{- if .Values.global.imagePullSecret.enabled }}
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret.name }}
      {{- end }}
      {{- include "app.spot.affinity" . | nindent 6 }}
      {{- include "app.spot.tolerations" . | nindent 6 }}
      containers:
        - name: {{ .Values.learningManager.name }}
          image: {{ include "app.image" (dict "name" .Values.learningManager.image.name "tag" .Values.learningManager.image.tag "Values" .Values) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.learningManager.ports.container }}
              protocol: TCP
          env:
            - name: ASPNETCORE_URLS
              value: "{{ .Values.learningManager.env.ASPNETCORE_URLS }}"
          resources:
            {{- toYaml .Values.learningManager.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.learningManager.name }}
  namespace: {{ include "app.namespace" . }}
spec:
  type: {{ .Values.learningManager.service.type }}
  selector:
    {{ .Values.learningManager.labels.selectorKey }}: {{ .Values.learningManager.labels.selectorValue }}
  ports:
    - port: {{ .Values.learningManager.ports.service }}
      targetPort: {{ .Values.learningManager.ports.container }}