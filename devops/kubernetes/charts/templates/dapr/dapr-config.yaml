apiVersion: v1
kind: ConfigMap
metadata:
  name: dapr-config
  namespace: {{ include "app.namespace" . }}
data:
  config.yaml: |
    apiVersion: dapr.io/v1alpha1
    kind: Configuration
    metadata:
      name: daprConfig
    spec:
      tracing:
        samplingRate: "1"
