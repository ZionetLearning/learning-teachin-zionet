apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: redis-connection
  namespace: {{ include "app.namespace" . }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault-backend
    kind: ClusterSecretStore
  target:
    name: redis-connection
  data:
    - secretKey: redis-hostport
      remoteRef:
        key: "redis-hostport"
    - secretKey: redis-password
      remoteRef:
        key: "redis-password"
