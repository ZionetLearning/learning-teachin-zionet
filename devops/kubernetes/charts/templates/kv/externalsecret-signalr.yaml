# SignalR now uses Managed Identity - this ExternalSecret is no longer needed
# apiVersion: external-secrets.io/v1
# kind: ExternalSecret
# metadata:
#   name: signalr-connection
#   namespace: {{ include "app.namespace" . }}
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: azure-keyvault-backend
#     kind: ClusterSecretStore
#   target:
#     name: signalr-connection
#   data:
#     - secretKey: SignalRConnectionString
#       remoteRef:
#         key: "{{ .Values.namespace.name }}-signalr-connection"
