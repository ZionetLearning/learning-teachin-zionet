apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.aiEngine.name }}
  namespace: {{ include "app.namespace" . }}
  labels:
    {{ .Values.aiEngine.labels.selectorKey }}: {{ .Values.aiEngine.labels.selectorValue }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{ .Values.aiEngine.labels.selectorKey }}: {{ .Values.aiEngine.labels.selectorValue }}
  template:
    metadata:
      labels:
        {{ .Values.aiEngine.labels.selectorKey }}: {{ .Values.aiEngine.labels.selectorValue }}
      annotations:
        {{ include "app.dapr.annotations" (dict
            "enabled" .Values.global.dapr.enabled
            "appId"   (.Values.aiEngine.dapr.appId | default .Values.aiEngine.name)
            "appPort" .Values.aiEngine.dapr.appPort
            "config"  (.Values.global.dapr.configName | default "dapr-config")
        ) | nindent 8 }}
        dapr.io/sidecar-cpu-request:  "{{ .Values.global.dapr.sidecar.requests.cpu }}"
        dapr.io/sidecar-memory-request: "{{ .Values.global.dapr.sidecar.requests.memory }}"
        dapr.io/sidecar-cpu-limit:    "{{ .Values.global.dapr.sidecar.limits.cpu }}"
        dapr.io/sidecar-memory-limit: "{{ .Values.global.dapr.sidecar.limits.memory }}"
    spec:
      {{- if .Values.global.imagePullSecret.enabled }}
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret.name }}
      {{- end }}
      {{- include "app.spot.affinity" . | nindent 6 }}
      {{- include "app.spot.tolerations" . | nindent 6 }}
      containers:
        - name: {{ .Values.aiEngine.name }}
          image: {{ include "app.image" (dict "name" .Values.aiEngine.image.name "tag" .Values.aiEngine.image.tag "Values" .Values) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.aiEngine.ports.container }}
              protocol: TCP
          env:
            - name: ASPNETCORE_URLS
              value: "{{ .Values.aiEngine.env.ASPNETCORE_URLS }}"
          resources:
            {{- toYaml .Values.aiEngine.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.aiEngine.name }}
  namespace: {{ include "app.namespace" . }}
spec:
  type: {{ .Values.aiEngine.service.type }}
  selector:
    {{ .Values.aiEngine.labels.selectorKey }}: {{ .Values.aiEngine.labels.selectorValue }}
  ports:
    - port: {{ .Values.aiEngine.ports.service }}
      targetPort: {{ .Values.aiEngine.ports.container }}