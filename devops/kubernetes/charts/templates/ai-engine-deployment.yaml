apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.ai-engine.name }}
  namespace: {{ include "app.namespace" . }}
  labels:
    {{ .Values.ai-engine.labels.selectorKey }}: {{ .Values.ai-engine.labels.selectorValue }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{ .Values.ai-engine.labels.selectorKey }}: {{ .Values.ai-engine.labels.selectorValue }}
  template:
    metadata:
      labels:
        {{ .Values.ai-engine.labels.selectorKey }}: {{ .Values.ai-engine.labels.selectorValue }}
      annotations:
        {{ include "app.dapr.annotations" (dict
            "enabled" .Values.global.dapr.enabled
            "appId"   (.Values.ai-engine.dapr.appId | default .Values.ai-engine.name)
            "appPort" .Values.ai-engine.dapr.appPort
            "config"  (.Values.global.dapr.configName | default "dapr-config")
        ) | nindent 8 }}
        dapr.io/sidecar-cpu-request:  "{{ .Values.global.dapr.sidecar.requests.cpu }}"
        dapr.io/sidecar-memory-request: "{{ .Values.global.dapr.sidecar.requests.memory }}"
        dapr.io/sidecar-cpu-limit:    "{{ .Values.global.dapr.sidecar.limits.cpu }}"
        dapr.io/sidecar-memory-limit: "{{ .Values.global.dapr.sidecar.limits.memory }}"
    spec:
      {{- if .Values.global.imagePullSecret.enabled }}
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret.name }}
      {{- end }}
      {{- include "app.spot.affinity" . | nindent 6 }}
      {{- include "app.spot.tolerations" . | nindent 6 }}
      containers:
        - name: {{ .Values.ai-engine.name }}
          image: {{ include "app.image" (dict "name" .Values.ai-engine.image.name "tag" .Values.ai-engine.image.tag "Values" .Values) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.ai-engine.ports.container }}
              protocol: TCP
          env:
            - name: ASPNETCORE_URLS
              value: "{{ .Values.ai-engine.env.ASPNETCORE_URLS }}"
          resources:
            {{- toYaml .Values.ai-engine.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.ai-engine.name }}
  namespace: {{ include "app.namespace" . }}
spec:
  type: {{ .Values.ai-engine.service.type }}
  selector:
    {{ .Values.ai-engine.labels.selectorKey }}: {{ .Values.ai-engine.labels.selectorValue }}
  ports:
    - port: {{ .Values.ai-engine.ports.service }}
      targetPort: {{ .Values.ai-engine.ports.container }}